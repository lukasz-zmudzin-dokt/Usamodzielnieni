export const downloadCV = (e, id) => {
    e.preventDefault();
    console.log("pobieram CV o id " + id);
};

export const acceptCV = (e, id) => {
    e.preventDefault();
    console.log("akceptuję CV o id " + id);
};

export const improveCV = (e, id) => {
    e.preventDefault();
    console.log("poprawiam CV o id " + id);
};