import React from "react";
import { Button } from "react-bootstrap";

const DeleteAccountButton = () => {
  return <Button variant="danger">Usuń konto</Button>;
};

export default DeleteAccountButton;
